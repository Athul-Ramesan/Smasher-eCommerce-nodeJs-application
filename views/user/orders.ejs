<%- include ('../layouts/header') %>
    <%- include ('../partials/userHeader') %>

        <style>
            .checkout .form-control {
                border-radius: 0px !important;
            }

            .checkout .form-control:focus {
                border: 1px solid #000 !important;
                box-shadow: none !important;
            }

            .checkout .nav-link {
                border: 1px solid #000;
                border-radius: 0px;
                margin-bottom: 8px;
            }

            .checkout .tab-content {
                padding-right: 10px;
            }
        </style>

        <div class="py-3 py-md-4 checkout">
            <div class="container">
                <div class="d-flex justify-content-between">
                    <h4>Orders</h4>
                    
                </div>

                <hr>

                <div class="row">

                    <% orders.reverse().forEach((order, index) => { %>
                        <a href="/orderDetails/<%= order._id %>" style="text-decoration: none;">
                      <div class="col-md-12 mb-3">
                        <div class="card shadow">
                          <div class="card-header d-flex justify-content-between align-items-center">
                            <h5 class="card-title text-muted m-2">OrderID. <%= order._id %></h5>

                            <% if(order.status === 'Cancelled'){ %>
                                <span class="text-danger">Order Cancelled </span>
                            <%}else{%>
                                <a href="/cancelOrder/<%= order._id %>" class=" btn btn-danger cancel-order" >Cancel Order</a>
                                <%}%>
                            
                          </div>
                          <div class="card-body">
                            <div class="d-flex">
                                <span class="m-1 font-weight-bold">Order Date: <%= order.orderedDate %></span>
                            </div>
                            <div class="d-flex">
                                <span class="m-1 font-weight-bold">Total Items: <%= order.items.length %></span>
                            </div>
                            <!-- <div class="d-flex">
                                <span class="m-1 font-weight-bold">Expected delivery Date: <%= order.deliveryDate %></span>
                            </div> -->
                            <span class="m-1 font-weight-bold">Status :<%= order.status %></span>
                          </div>
                        </div>
                      </div>
                    </a>
                    <% }) %>
                  </div>
                  
                      
                  
                  
                  
                 
        

<script>
 
    document.addEventListener("DOMContentLoaded", () => {
                const cancelOrder = document.querySelectorAll('.cancel-order')
                cancelOrder.forEach((button) => {
                    button.addEventListener('click', (event) => {
                        event.preventDefault()

                        Swal.fire({
                            title: 'Are you sure?',
                            text: "You won't be able to revert this!",
                            icon: 'warning',
                            showCancelButton: true,
                            confirmButtonColor: '#3085d6',
                            cancelButtonColor: '#d33',
                            cancelButtonText: 'No',
                            confirmButtonText: 'Yes, Cancel it!'
                        }).then((result) => {
                            if(result.isConfirmed){
                                window.location.href = button.href
                            }
                            
                        })
                    })
                })
                
                
            })
            function formatDate(date) {
    const options = { year: 'numeric', month: 'short', day: 'numeric' };
    return new Date(date).toLocaleDateString(undefined, options);
  }
     // Define a function to format the date using Moment.js
    

</script>
        <%- include ('../partials/userFooter') %>
            <%- include ('../layouts/footer') %>